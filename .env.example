# Optional shared secret between the mobile/web client and the backend.
# Must match the backend's API_KEY env var (set on Railway).
# Leave empty to disable the protection (open API).
EXPO_PUBLIC_API_KEY=
